# Piper 操作速查

> 2026-09-20：SmolVLA 已完成训练和真机初步验证。红色任务有成功案例，白色指令仍可能抓错颜色，动作抖动也待改进。完整的采集、导出、训练、下载和推理命令见 [SMOLVLA.md](SMOLVLA.md)；当前控制程序的暂停/自动回位等已知问题见 [项目记录](docs/PROJECT_NOTES.md#control-code-status)。下面保留原实验台的操作记录，路径、相机编号和姿态需要按实际机器核对。

台前用的命令卡。设计说明在 `README.md`,硬件测试流程在 `TESTING.md`。

所有脚本都在 `~/Desktop/Agilex_Piper/sdk_tests`,相机在 `~/Desktop/Agilex_UMI`。

---

## 1. 开工

```bash
# CAN（每次开机、拔插扩展坞之后都要做一遍）
sudo ip link set can0 type can bitrate 1000000
sudo ip link set can0 up

# 确认通了：应该看到固件版本、六个关节角、enabled 全 False
cd ~/Desktop/Agilex_Piper/sdk_tests
python3 arm_tool.py status

# 摆到对着桌子的工作姿态（j1=-90 是这台机器的工作区方向）
python3 joint_position_ctrl.py --target -90 60 -45 0 30 0
```

## 2. 遥操作

遥操作脚本自己会开相机、画 HUD,**不要再单独跑 `view_cameras.py`,会抢相机**。

```bash
cd ~/Desktop/Agilex_Piper/sdk_tests
python3 teleop_spacemouse.py --view                    # 带 HUD 的第一视角(不录)
python3 teleop_spacemouse.py                           # 标准模式(默认),NORMAL 挡 12 cm/s
python3 teleop_spacemouse.py --frame tool              # FPS 视角:方向跟着夹爪走
python3 teleop_spacemouse.py --lin-speed 0.05 --max-joint-vel 0.5   # 想慢下来
```

**挡位**(运行中 `[` `]` 切换,不用重启)

| 挡 | 平移 | 旋转 | 用途 |
|---|---|---|---|
| PRECISE | 2.5 cm/s | 11°/s | 怼方块、最后对准 |
| SLOW | 5 cm/s | 22°/s | 接近物体 |
| **NORMAL** | **10 cm/s** | **45°/s** | 默认,滞后只有 3 mm |
| FAST | 14 cm/s | 63°/s | 横穿桌面 |
| TURBO | 18 cm/s | 81°/s | 到跟踪极限,工具可能歪（HUD 上看 `tilting`） |

**两种控制系**(运行中按 `m` 随时切换,不用重启)

| 模式 | 推球向前 = | 什么时候好用 |
|---|---|---|
| `--frame base`（**默认**,标准） | 桌面上一个固定方向 | 大范围移动、对不准的时候 |
| `--frame tool`（FPS） | 夹爪自己朝的方向 | 对准物体、贴着物体走 |

标准模式的"前"默认取**机械臂自己的朝向**(启动时的 joint1,这台是 −90°),所以推球向前 =
往桌子那边伸。要自己指定就加 `--frame-yaw <角度>`。

**SpaceMouse**

| 操作 | 效果 |
|---|---|
| 推 / 提 / 拉 | 末端平移（方向按上面的模式） |
| 扳 / 扭 | 绕 TCP 旋转（**要先按 `r` 打开**） |
| 左键 | 夹爪张开到 70 mm |
| 右键 | 夹爪合到 20 mm |

**键盘**（终端要有焦点）

| 键 | 作用 |
|---|---|
| **空格** | **暂停 / 恢复**(恢复时自动重锚) ← 卡住了先按这个 |
| `a` | 重锚：目标回到机械臂实际位置 |
| `r` | 旋转 开 / 关（默认关） |
| `t` | 平移 开 / 关 |
| `1` | 严格单轴：只有最大的轴生效，对准时好用 |
| `[` `]` | **换挡**:PRECISE → SLOW → NORMAL → FAST → TURBO |
| `w` / `g` | 存航点 / 飞回航点 |
| `h` | 飞回开机时的姿态（迷路了按这个） |
| `m` | 切换 FPS / 标准控制系 |
| `f` | 输入方向转 90°,对齐相机画面 |
| `R` / `X` | 开始·结束录制 / 丢弃这一条（要 `--record`） |
| `T` | 切换任务（给了多个 `--task` 时；录制中不让切） |
| `q` | 退出（保持姿态，不失能） |

**画面上的 HUD**(眼睛盯着相机就够了，不用回头看终端)

| 位置 | 显示 |
|---|---|
| 顶部 | 控制系 / 平移·旋转开关 / 当前挡位 / `HOLD` 红牌 / 录制计时 |
| 左上 JOINTS | 六个关节还剩多少余量,**变红 = 那个关节快到头了** |
| 右上 INPUT | SpaceMouse 实际发出的方向（箭头朝上 = 往远离你的方向） |
| 左下 GRIPPER | 实测宽度（蓝条）+ 指令宽度（白线） |
| 底部 | 任务描述 / TCP 坐标 / `lag`(跟不跟得上) / `tilting`(在放姿态) |
| 中间 | 最近一条提示，3 秒后消失 |

> HUD 只画在窗口上，**录进 mp4 的画面是干净的**。

## 3. 卡住了怎么办

先看状态行前缀：`[JOG:...]` 是在跟随，`[HOLD:...]` 是暂停了。

| 现象 | 原因 | 解法 |
|---|---|---|
| 前缀是 `HOLD` | 碰撞检测 / 漂移保护触发了 | **按空格**恢复 |
| 提示 `out of reach`,状态行出现 `ori 8%` | joint5(±70°,最窄)转到头了，脚本正在**放弃姿态、保位置**,夹爪会歪 | 让它歪着走完，或者往回退一点姿态会自己转回来；要它别歪就 `--ori-min 0.4` |
| 提示 `too close to the base` | 离底座竖轴 < 25 cm,那里手臂会折回来、j1/j5 一起顶限位 | **往外走**。工作区要摆在离底座 25 cm 以外 |
| 提示 `workspace box is blocking` | 撞到工作空间边界 | 往回推，或重启时放宽 `--box-*`(z 的地板现在是 −0.05 m,底座平面 = 桌面 = 0) |
| 推了没反应，也没提示 | 推得太轻（指数曲线，轻推接近静止） | 用力推到底试试 |
| 提示 `collision?` | 力矩偏离模型 > 3 N·m | **按空格**——恢复后碰撞检测会关 2 s,趁这 2 s 往回开出去。顶在限位上的关节已经不算碰撞了；还是误报就 `--collision-torque 5` |

### 多任务录制

`--task` 可以给多次，运行中按 `T` 切换，**不用重启**:

```bash
python3 teleop_spacemouse.py --record ~/piper_data/cubes \
    --task "put the red cube in the black box" \
    --task "put the white cube in the black box"
```

每条 episode 的 `meta.json` 存的是**按 R 那一刻**的任务。录制中按 `T` 会被拒绝
（会提示先按 R 停）,不会出现标错的数据。

**措辞规矩**（很重要，别临场发挥）:

- 一个任务**只用一种固定说法**,不要换着说。改写是训练时做的数据增强，不是采集时做的
- 用**祈使句 + 动词开头**:`put the red cube in the black box` ✓
  不要用目标状态描述:`make the black box contain a red cube` ✗
- 多个任务之间**只让该变的词变**(颜色),其他一个字都不要动——
  这样模型唯一能区分两个任务的线索就是颜色

## 3.5 说不清为什么不动？录一段 trace

```bash
# 单独诊断(不录数据)
python3 teleop_spacemouse.py --view --trace /tmp/run.jsonl
python3 analyse_trace.py /tmp/run.jsonl
python3 analyse_trace.py /tmp/run.jsonl --axis z     # 只看某个轴
```

**录数据的时候自动就有 trace**,存成 `<录制目录>/session-<时间>.trace.jsonl`,不用加参数。

trace 按 50 Hz 记录控制环里的一切：推杆输入、目标位置、实际 TCP、滞后、
哪个轴被工作空间盒挡了、姿态权重、六个关节角、挡位、控制系。
`analyse_trace.py` 会算出**每个轴"要了多少 / 给了多少"**,并把没给的时间按原因归类：

```
  z: asked  119.8 cm, delivered   20.1 cm  (  17%)
          8.3 s  workspace box, lower z limit
        the z box is [0.02, 0.75] m and the target sits at +0.021
        -- raise it with --box-z -0.08 0.75
```

（episode 里的 `data.jsonl` 只有关节角和夹爪，**不够诊断**,所以才有这个。）

## 4. 收工

```bash
# 1. 遥操作按 q 退出，相机 Ctrl+C

# 2. 清空桌面，回折叠零位
python3 joint_position_ctrl.py --zero

# 3. 这时候才能安全失能
python3 arm_tool.py disable
```

**零位是唯一能放心失能的姿态**——重力把 j2/j3 压在机械硬限位上，断电也不会动。

## 5. 哪些命令会让机械臂掉下来

| 命令 | 作用 | 会掉吗 |
|---|---|---|
| 遥操作里按 `q` / 空格 | 退出 / 暂停 | **不会**,固件保持最后姿态 |
| `arm_tool.py disable` | 电机失能 | **会**,除非在零位 |
| `arm_tool.py stop` | 固件急停 | **会** |
| `arm_tool.py reset` | 清故障 / 退出 MIT 模式 | **会**(瞬间掉电) |

非零位又必须断电时：**一只手托住小臂**,另一只手跑 `arm_tool.py disable -y`。

## 6. 出故障

```bash
python3 arm_tool.py status     # 故障码 + 每个关节驱动器的使能/温度/报警位
python3 arm_tool.py stop       # 固件急停
python3 arm_tool.py reset      # 清错误、退出 MIT/示教模式（会掉电，在零位做）
```

### 某个关节"绿灯亮着但是没力气"

单个关节驱动器自己触发保护、自己失能了。**整机 `arm_status` 还是 NORMAL**,只有
`arm_tool.py status` 的关节表能看出来：

```
joint5 OFF      23.0V   35C   30C  collision_status, driver_error_status
       ^^^ 驱动器失能 = 没力气                   ^^^ 碰撞保护触发
```

（`motor_overheating` 经常和错误位一起亮，电机温度才 30 °C 的话不用理它。）

恢复。**顺序很重要**:`enable` 单独用没用,驱动器的错误码是锁存的,要先清；
`reset` 能清但是整机掉电会摔。`clear` 只清错误码，不掉电：

```bash
# 1. 一只手托住小臂（那个关节是软的）
python3 arm_tool.py clear --joint 5        # 清锁存的错误码,不掉电
python3 arm_tool.py enable                 # 这时候才能使能
python3 arm_tool.py status                 # 确认 joint5 那行变成 ON、faults 是 -

# 2. 清完还是 OFF 才用 reset(整机掉电,务必托住)
python3 arm_tool.py reset && sleep 3 && python3 arm_tool.py enable

# 3. 回工作姿态,把关节从限位附近挪开
python3 joint_position_ctrl.py --target -90 60 -45 0 30 0
```

老是触发的话，把那个关节的碰撞防护调松一点（出厂 J4–J6 是 **1**,最灵敏）：

```bash
python3 arm_tool.py protect                    # 看当前等级
python3 arm_tool.py protect --set 4 3 3 3 3 3  # J4-J6 从 1 提到 3
```

0 = 完全不检测碰撞，别设 0——脚本自己那层 3 N·m 的检测防不住关节堵转。

- **急停之后**:`reset` → 等 3 秒 → 重新使能（脚本会自己使能）
- **跑完重力补偿或阻抗之后**,必须 `reset` 才能回到位置控制，否则发指令没反应
- `JOINT_COMMUNICATION_ERR` 通常是 reset 后使能太早的瞬时故障，重试即可

## 7. 其他工具

```bash
python3 arm_tool.py status              # 整机状态 + 六个驱动器的健康表（只读）
python3 read_joints.py                  # 实时看关节 / 速度 / 力矩（只读，安全）
python3 monitor.py --effort-x4 --csv x.csv   # 抖动诊断：高频幅度、主频、力矩峰峰
python3 gravity_compensation.py         # 零力矩拖动（MIT 模式）
python3 impedance.py                    # 笛卡尔阻抗（推末端会弹回）
```

MoveIt:

```bash
cd ~/Desktop/Agilex_Piper/ros2_ws && source install/setup.bash
ros2 launch piper_moveit_config piper_moveit.launch.py use_sim:=false
ros2 run piper_moveit_config moveit_goal.py --named ready
```

## 8. 这台机器的固定参数

写死的事实，不用每次问：

| 项目 | 值 |
|---|---|
| 工作区方向 | **joint1 = −90°**(桌子在底座 −Y 方向) |
| **工作区最小半径** | **离底座竖轴 25 cm 以外**,再近 IK 会把手臂折回来,j1 跑到 −148°、j5 到 67°,直接把 j5 驱动器干自锁 |
| **摆东西的甜区** | **半径 0.30–0.38 m**。夹爪竖直朝下时 j5 需要：0.32 m→52°,0.36 m→56°,0.40 m→61°,0.45 m→69°(必炸) |
| **joint5 实测保护点** | **约 65°**,远在标称 69.9° 以内。所以命令上限压到 **±60°**(`--limit-margin 2 2 2 2 10 2`) |
| 固件 | S-V1.7-3 → 力矩反馈要 `--effort-x4`,MIT 可用 |
| 末端 | Pika Gripper → `--tool pika`(默认) |
| 重力模型负载 | `--payload 0.42 --payload-x 0.03 --payload-z 0.07`(默认已带) |
| 最窄关节 | **joint5,±70°**,操作中最先用完 |
| 相机 | D405 = `/dev/pika_gripper` 上，序列号 315122271085 是 Sense 的、315122272699 是 Gripper 的 |
| 鱼眼稳定编号 | Gripper `/dev/video60`,Sense `/dev/video50` |
| CAN 适配器 | 扩展坞 `5-9.4`,拔插后必须重新激活 |
